[general]
; Emit completed CDR records over the already authenticated AMI connection.
; This is consumed by the synthetic acceptance suite and operational tooling;
; it does not imply billing or carrier-grade CDR suitability.
enabled = yes
