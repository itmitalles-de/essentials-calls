[general]
static=yes
writeprotect=no
clearglobalvars=no

[globals]

; Dialplan contexts (internal, entrypoints, callflow, ivr_*) are generated by
; the backend from the topology graph.
#include generated/current/extensions_generated.conf
