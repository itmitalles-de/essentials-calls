[global]
type=global

[transport-udp]
type=transport
protocol=udp
bind=0.0.0.0:5060

; Endpoints/AORs/auth for each extension are generated by the backend from
; the topology and written to generated/pjsip_generated.conf.
#include generated/pjsip_generated.conf
